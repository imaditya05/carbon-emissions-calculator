# Application Configuration
# Rename this file to .env and update the values

APP_NAME="Carbon Emission Calculator"
APP_VERSION="1.0.0"
DEBUG=true
ENVIRONMENT="development"

# Server Configuration
HOST="0.0.0.0"
PORT=8000

# MongoDB Configuration
MONGODB_URL="mongodb+srv://username:password@cluster.mongodb.net/"
MONGODB_DATABASE="carbon_emission_db"

# JWT Authentication
# IMPORTANT: Change this to a secure random string in production!
# Generate with: openssl rand -hex 32
JWT_SECRET_KEY="your-super-secret-key-change-in-production"
JWT_ALGORITHM="HS256"
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Mapbox API (for routing and geocoding)
# Get your access token from: https://account.mapbox.com/
MAPBOX_ACCESS_TOKEN=""

# CORS Configuration
# For local development with Vite (default ports)
# For Cloud Run, add your frontend URL: https://carbon-frontend-xxxxx-uc.a.run.app
CORS_ORIGINS='["http://localhost:3000", "http://localhost:5173", "http://127.0.0.1:5173"]'
